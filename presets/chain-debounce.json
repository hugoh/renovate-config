{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Hold frequently released deps to the same Friday-evening window as the weekly patch group, so they don't add a second round of updates on the weekend.",
  "extends": ["local>hugoh/renovate-config//presets/weekend"]
}
