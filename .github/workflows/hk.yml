---
name: hk

on: # yamllint disable-line rule:truthy
  push:
    branches:
      - main
  pull_request:

permissions: {}

jobs:
  hk:
    uses: hugoh/gh-workflows/.github/workflows/hk.yml@98894bfdb331f6bfc3e1b7a1d1ddbe637080a031 # v1.40.4
    permissions:
      contents: read
      packages: read
      statuses: write
