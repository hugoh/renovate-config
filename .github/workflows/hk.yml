---
name: hk

on: # yamllint disable-line rule:truthy
  push:
    branches:
      - main
  pull_request:

permissions: {}

jobs:
  hk:
    uses: hugoh/gh-workflows/.github/workflows/hk.yml@2d58c2d886399dca718e4de89f8c7d0ccd2c749e # v1.38.0
    permissions:
      contents: read
      packages: read
      statuses: write
